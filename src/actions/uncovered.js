export const uncovered = async (year) => {
  if (year === 2022) {
    throw new Error();
  }

  if (year == null) {
    throw new Error();
  }

  const z = 20;
  const q = z + year;
  const p = q * z;
  console.log(p);
};

export const uncovered2 = async (year) => {
  if (year === 2022) {
    throw new Error();
  }

  if (year == null) {
    throw new Error();
  }

  const z = 20;
  const q = z + year;
  const p = q * z;
  console.log(p);
};

export const uncovered3 = async (year) => {
  if (year === 2022) {
    throw new Error();
  }

  if (year == null) {
    throw new Error();
  }

  const z = 20;
  const q = z + year;
  const p = q * z;
  console.log(p);
};
