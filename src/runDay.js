import { logger } from './logger.js';

const getPathToSolution = '';


/**
 * Runs the solution for the given day.
 * @param {Number} year
 * @param {Number} day
 */
export const runDay = async (year, day, input) => {
  logger.verbose('running solution for year: %s, day: %s', year, day);

  return null;
};
